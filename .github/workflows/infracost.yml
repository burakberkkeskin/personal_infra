name: 'Infracost Analysis'
on:
  pull_request:
    branches:
      - master
jobs:
  infracost:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: infracost
        uses: ./infracost-action.yml
        with:
          working-directory: ./main
          terraform-var-file: ${{ secrets.TFVARS_MASTER }}
          usagefile: ./infracost-usage.yml
